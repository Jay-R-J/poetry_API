# 服务监听地址与端口
HOST=127.0.0.1
PORT=8080

# SQLite 数据库路径（相对项目根目录）
DATABASE_URL=sqlite://data/poems.db

# 可选：设置后开启 API Key 认证（留空则为开放模式）
# 请求时携带 X-API-Key: <key> 或 Authorization: Bearer <key>
API_KEY=

# 限流：每个 IP 在窗口内允许的最大请求数
RATE_LIMIT=100
RATE_WINDOW_SECS=60

# 日志级别
RUST_LOG=poem_api=debug,info
